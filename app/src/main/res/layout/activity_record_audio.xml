<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_record_audio"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/coolColor"
    tools:context="com.example.aleksandrromanov.audiowizard.RecordAudio">

    <Button
        android:text="Tap to Record"
        android:layout_width="@android:dimen/thumbnail_width"
        android:layout_height="wrap_content"
        android:id="@+id/record_button"
        android:layout_marginBottom="134dp"
        android:layout_above="@+id/stop_button"
        android:background="@color/buttonColor"
        android:layout_centerHorizontal="true" />

    <Button
        android:text="Play"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@color/buttonColor"
        android:id="@+id/play_button"
        android:layout_below="@+id/record_button"
        android:layout_toEndOf="@+id/record_state" />

    <Button
        android:text="Stop"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/stop_button"
        android:background="@color/buttonColor"
        android:layout_marginBottom="22dp"
        android:layout_alignParentBottom="true"
        android:layout_toStartOf="@+id/record_state" />

    <TextView
        android:text="Recording ..."
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/record_state"
        android:visibility="invisible"
        android:textColor="@color/textColor"
        android:layout_marginTop="82dp"
        android:layout_alignTop="@+id/record_button"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
